{
  "name": "rally-social",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "RALLY social render harness — Node + TypeScript + Playwright. All values come from canon.ts.",
  "scripts": {
    "render": "node --experimental-strip-types src/render.ts",
    "check": "node --experimental-strip-types src/check.ts",
    "build": "npm run render && npm run check"
  },
  "dependencies": {
    "playwright": "^1.56.1"
  }
}
